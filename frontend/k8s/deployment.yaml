apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: frontend
    environment: dev
  annotations:
  name: devops-frontend
  namespace: dev
spec:
  selector: 
    matchLabels:
      app: frontend
      environment: dev
  replicas: 1
  strategy:
    rollingUpdate:
      maxUnavailable: 33%
      maxSurge: 50%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: frontend
        environment: dev        
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - devops-frontend
            topologyKey: "kubernetes.io/hostname"   
      serviceAccountName: devops-frontend
      containers:
      - name: devops-frontend
        image: 836855437023.dkr.ecr.ap-south-1.amazonaws.com/examplecode-ecr-repo:frontend-latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: api
        resources:
          requests:
            cpu: 600m
            memory: 1105Mi
          limits:
            cpu: 600m
            memory: 1105Mi

